# 能力特化层（Capability / Specialization）

用户特化 Agent 能力的对外接口，与缓存**解耦**（见 ADR-001）。

可插拔的四类能力：
- 上下文：注入项目/团队背景、编码规范、领域知识、私有文档索引
- 工具：注册本项目可用的工具（内部 API、构建脚本、MCP Server）
- 提示：覆盖或扩展系统提示、输出风格、评审清单
- 检索：自定义检索与召回（语义检索、权限过滤）

设计原则：不侵入编排、可组合（按优先级叠加）、可回退（策略缺失/出错回退默认）、按作用域生效（全局 / 项目 / 子树 / 单任务）。
