# 本地 API 管理层（Model Access）

远程 API + 密钥/端点本地管理（见 ADR-002）。

职责：
- 密钥的本地保存与读取（发布版走系统钥匙串，开发运行由环境变量注入）
- 端点（base_url）与模型名统一配置
- 面向不同 Agent 构建模型客户端
- 密钥脱敏与防泄露：不进入图状态、不打印、不进日志、不进 SQLite
- 无密钥时可显式选择 mock 模式；缺密钥必须明确失败，不得静默回退
