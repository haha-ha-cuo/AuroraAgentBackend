# AuroraAgent 工程化规范化建议文档

> 建议日期: 2026-08-28  
> 适用范围: AuroraAgentFrontend + AuroraAgentBackend 双仓库
> 实施状态: 2026-08-29 已完成；下文“现状”保留为改造前审计基线。

已落地目录命名与 stdio/WebSocket 链路、双仓库 setup/Makefile、环境模板、CI、Ruff/ESLint、Pyright、提交钩子、统一文档、ADR、安全扫描、CSP、依赖审计、覆盖率与 E2E、协议版本集中管理、MIT 许可证、社区模板，以及基于 Release Please 的 SemVer/CHANGELOG/tag 自动发布链路。

---

## 一、仓库与目录结构规范化

### 1.1 统一仓库命名约定

**现状**: 前端硬编码期望后端目录名为 `AuroraApp`，但实际仓库名为 `AuroraAgentBackend`。

**建议**:
- 前端 `dev-backend.ts` 中将默认路径从 `../../AuroraApp` 改为 `../../AuroraAgentBackend`，或使用环境变量作为唯一配置方式。
- 在两个仓库的 README 中明确标注推荐的目录布局：

```
workspace/
├── AuroraAgentFrontend/    # 前端仓库
└── AuroraAgentBackend/     # 后端仓库
```

### 1.2 引入 Monorepo 或 Workspace 管理

**现状**: 两个独立仓库通过 `AURORA_ROOT` 环境变量关联，缺少统一的依赖管理和启动脚本。

**建议方案 A（推荐）**: 引入顶层 `Makefile` 或 `justfile`：

```makefile
# Makefile (放在某个仓库或独立的 devops 仓库)
.PHONY: setup dev-backend dev-frontend dev-all test-all

setup:
	cd AuroraAgentBackend && uv sync
	cd AuroraAgentFrontend && pnpm install

dev-backend:
	cd AuroraAgentBackend && uv run aurora runtime --port 8765

dev-frontend:
	cd AuroraAgentFrontend && pnpm dev

dev-all:
	@echo "Starting backend on :8765..."
	$(MAKE) dev-backend &
	sleep 2
	$(MAKE) dev-frontend

test-all:
	cd AuroraAgentBackend && uv run pytest
	cd AuroraAgentFrontend && pnpm test
```

**建议方案 B**: 使用 `pnpm-workspace.yaml` 将后端作为 workspace 的一部分（需要后端也放在同一 monorepo 下），但这会改变 git 仓库结构，需评估。

### 1.3 自动化 Setup 脚本

**建议**: 在前端仓库根目录添加 `scripts/setup.sh`：

```bash
#!/usr/bin/env bash
set -euo pipefail

# 前端依赖
pnpm install

# 后端依赖
BACKEND_DIR="${AURORA_ROOT:-$(dirname "$PWD")/AuroraAgentBackend}"
if [ ! -d "$BACKEND_DIR" ]; then
  echo "错误: 后端目录不存在: $BACKEND_DIR"
  echo "请设置 AURORA_ROOT 环境变量"
  exit 1
fi

cd "$BACKEND_DIR"
cp -n .env.example .env || true
uv sync

echo "Setup 完成。请编辑 $BACKEND_DIR/.env 填写 API 密钥。"
```

---

## 二、环境变量与配置规范化

### 2.1 统一 `.env` 模板

**现状**: 两个仓库各有 `.env.example`，但前端的 `.env.example` 过于简略（只有2个变量），且未说明 `VITE_RUNTIME_WS` 的完整行为。

**建议**: 前端 `.env.example` 扩展为：

```bash
# ============================
# AuroraAgent 前端环境变量
# ============================

# Python 后端目录绝对路径。
# 默认使用与 frontend 同级的 AuroraAgentBackend。
# AURORA_ROOT=/absolute/path/to/AuroraAgentBackend

# WebSocket 运行时地址。
# 设置后 dev-backend 模块不再自动拉起后端进程，
# 需要手动启动后端: uv run aurora runtime --port <port>
# VITE_RUNTIME_WS=ws://127.0.0.1:8765/ws
```

### 2.2 修复 `VITE_RUNTIME_WS` 跳过逻辑

**现状**: `dev-backend.ts` 中：

```ts
if (process.env.VITE_RUNTIME_WS && process.env.VITE_RUNTIME_WS !== DEFAULT_WS) return
```

只有地址不同于默认值时才跳过自动拉起，这不符合用户直觉。

**建议**: 改为：

```ts
if (process.env.VITE_RUNTIME_WS) return
```

只要用户显式设置了 WS 地址，就认为用户在手动管理后端。

### 2.3 后端 `.env` 的 `AGENT_API_KEY` 文档

**现状**: `.env.example` 中 `AGENT_API_KEY=` 为空，缺少获取密钥的指引。

**建议**: 添加注释说明支持的 API 提供商和获取方式：

```bash
# API 密钥（支持 OpenAI 兼容端点）
# - OpenAI: https://platform.openai.com/api-keys
# - 兼容服务: DeepSeek, Moonshot, 智谱 等
# - 本地模型: Ollama, vLLM 等需要设置 AGENT_BASE_URL
AGENT_API_KEY=

# 模型名示例:
# - OpenAI: gpt-4o, gpt-4o-mini
# - DeepSeek: deepseek-chat
# - 本地: qwen2.5:7b (Ollama)
AGENT_MODEL=
```

---

## 三、CI/CD 规范化

### 3.1 引入 GitHub Actions

**现状**: 两个仓库均无 CI 配置。

**建议**: 在每个仓库添加 `.github/workflows/ci.yml`：

**后端 CI**:

```yaml
name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.13', '3.14']
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v4
        with:
          version: '0.12'
      - run: uv sync
      - run: uv run pytest -v
      - run: uv run python -c "from aurora.agent.transport import serve_websocket"
```

**前端 CI**:

```yaml
name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm typecheck
      - run: pnpm test
```

### 3.2 版本锁定策略

**现状**: `pyproject.toml` 使用 `>=` 宽松约束，`pnpm-lock.yaml` 存在但 Python 端没有 lockfile。

**建议**:
- 后端已使用 `uv.lock`，保持现状。
- 前端 `pnpm-lock.yaml` 已存在，确保 CI 中使用 `pnpm install --frozen-lockfile`。
- Python 依赖建议收紧上界：`websockets>=16,<17`（避免大版本 API 变更）。

---

## 四、代码质量规范化

### 4.1 Lint 与格式化

**现状**: 两个仓库均无 linter/formatter 配置。

**建议**:

**后端** (`pyproject.toml`):

```toml
[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]

[tool.ruff.format]
quote-style = "double"
```

添加开发依赖：

```toml
[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.8",
]
```

**前端** (添加 `.eslintrc.cjs` 或使用 `@nuxt/eslint-config`):

```bash
pnpm add -D @nuxt/eslint-config eslint
```

### 4.2 Pre-commit Hooks

**建议**: 引入 `pre-commit` 或 `lefthook`：

```yaml
# lefthook.yml
pre-commit:
  commands:
    ruff:
      glob: "*.py"
      run: ruff check --fix {staged_files} && ruff format {staged_files}
    eslint:
      glob: "*.{ts,vue}"
      run: pnpm eslint --fix {staged_files}
```

### 4.3 类型安全

**现状**: 前端 TypeScript 类型定义集中在 `app/types/agent.ts`，后端 Python 使用 type hints 但无 mypy/pyright 配置。

**建议**:
- 后端添加 `pyrightconfig.json` 或在 CI 中运行 `pyright --verifytypes aurora`。
- 前端 `pnpm typecheck` 已通过，建议在 CI 中强制执行。

---

## 五、文档规范化

### 5.1 统一 README 结构

**现状**: 后端 README 内容丰富（190行），前端 README 较简略（77行）。两个 README 的信息组织方式不同。

**建议**: 统一为以下结构：

```markdown
# 项目名

一句话描述

## 快速开始
### 前置条件
### 安装
### 运行

## 架构概览
## 目录结构
## 开发指南
## 测试
## 部署
## 常见问题
```

### 5.2 添加 CONTRIBUTING.md

**建议**: 创建 `CONTRIBUTING.md`，覆盖：

- 开发环境搭建步骤（包括两个仓库的关联）
- 分支命名约定
- Commit message 格式（建议 Conventional Commits）
- PR 模板和 review 流程
- Issue 模板（bug report / feature request）

### 5.3 添加 CHANGELOG.md

**建议**: 使用 `changesets` 或 `conventional-changelog` 自动生成变更日志，尤其在发布初期建立好习惯。

### 5.4 ADR（架构决策记录）

**现状**: 后端 README 提到了 ADR-002、ADR-003 等，但文档存放在外部站点。

**建议**: 在仓库内创建 `docs/adr/` 目录，存放架构决策记录：

```
docs/adr/
├── 001-protocol-version.md
├── 002-no-mock-fallback.md
├── 003-no-langchain-meta-package.md
└── README.md
```

---

## 六、安全规范化

### 6.1 API 密钥管理

**现状**: 密钥通过 `.env` 文件管理，Tauri 桌面端使用系统 keyring。`.gitignore` 已忽略 `.env`。

**建议**:
- 在 README 中明确警告：**绝不要将真实 API 密钥提交到 git**。
- 后端 `.env.example` 中所有密钥字段应为空值。
- 考虑在 CI 中添加 `gitleaks` 或 `trufflehog` 扫描。

### 6.2 CSP 配置

**现状**: `tauri.conf.json` 中 `"csp": null`（禁用 CSP）。

**建议**: 在生产构建中启用 CSP：

```json
"security": {
  "csp": "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self' ws://127.0.0.1:*"
}
```

### 6.3 依赖审计

**建议**: 在 CI 中添加依赖审计步骤：

```yaml
- name: Audit Python dependencies
  run: uv pip audit
  
- name: Audit Node dependencies
  run: pnpm audit
```

---

## 七、测试规范化

### 7.1 测试覆盖率

**现状**: 后端66个测试，前端5个测试。前端测试覆盖率明显不足。

**建议**:
- 前端为每个 store（`runtimeStore`, `sessionStore`, `projectStore`, `uiStore`）编写单元测试。
- 前端为 `runtimeClient.ts` 的 WebSocket 连接逻辑编写测试。
- 后端为 `websocket.py`（新增文件）补充测试。
- CI 中设置最低覆盖率门槛。

### 7.2 集成测试

**建议**: 添加端到端测试：

```bash
# 后端集成测试：启动 runtime，发送 WebSocket 请求，验证响应
cd AuroraAgentBackend && uv run pytest tests/integration/ -v

# 前端 E2E 测试（使用 Playwright 或 Cypress）
cd AuroraAgentFrontend && pnpm test:e2e
```

### 7.3 测试命名约定

**现状**: 后端测试文件命名为 `test_*.py`，遵循 pytest 约定。

**建议**: 保持一致，所有新测试文件使用 `test_*.py` 前缀。前端测试文件使用 `*.spec.ts` 后缀（已有的约定）。

---

## 八、发布与版本规范化

### 8.1 语义化版本

**建议**: 两个仓库从 v0.1.0 开始严格遵循 SemVer：

- `MAJOR`: 协议版本不兼容变更
- `MINOR`: 新功能（如新 MCP 包、新命令）
- `PATCH`: Bug 修复

### 8.2 Git Tag 与 Release

**建议**:
- 使用 GitHub Releases 发布版本。
- 前端 Tauri 构建产物通过 GitHub Release 分发。
- 后端 Python 包考虑发布到 PyPI（`pip install auroraapp`）。

### 8.3 协议版本管理

**现状**: `PROTOCOL_VERSION = "1"` 在 `transport/api.py` 中硬编码。

**建议**: 将协议版本提取到 `pyproject.toml` 或独立的 `_version.py`，并在前端 `types/agent.ts` 中同步定义，方便跨版本兼容性检查。

---

## 九、社区化建议

### 9.1 Issue 模板

**建议**: 在 `.github/ISSUE_TEMPLATE/` 下添加：

- `bug_report.md`: 包含环境信息、复现步骤、期望行为
- `feature_request.md`: 包含用例、方案建议
- `question.md`: 使用场景和已查阅的文档

### 9.2 PR 模板

**建议**: 添加 `.github/pull_request_template.md`：

```markdown
## 变更内容

## 关联 Issue

## 测试
- [ ] 单元测试通过
- [ ] 手动验证通过

## 截图（如适用）
```

### 9.3 许可证

**现状**: 两个仓库均未添加 LICENSE 文件。

**建议**: 尽早添加许可证（如 MIT、Apache 2.0），避免后续法律风险。

### 9.4 行为准则

**建议**: 添加 `CODE_OF_CONDUCT.md`，采用 Contributor Covenant 或类似标准。

---

## 十、优先级排序

| 优先级 | 事项 | 工作量 | 影响 |
|--------|------|--------|------|
| P0 | 修复 `VITE_RUNTIME_WS` 跳过逻辑 | 1行 | 消除端口冲突 |
| P0 | 添加 LICENSE | 0.5h | 法律合规 |
| P1 | 添加 CI（GitHub Actions） | 2h | 质量保障 |
| P1 | 添加 Ruff + ESLint | 1h | 代码风格一致 |
| P1 | 统一 README 和 setup 脚本 | 2h | 新贡献者体验 |
| P2 | 前端测试补充 | 4h | 覆盖率 |
| P2 | 添加 CONTRIBUTING.md | 1h | 社区规范 |
| P2 | 依赖审计 CI | 0.5h | 安全 |
| P3 | ADR 文档迁入仓库 | 2h | 文档完整性 |
| P3 | Pre-commit hooks | 1h | 开发体验 |
| P3 | CHANGELOG 自动化 | 1h | 版本管理 |

---

## 附录：当前环境验证结果

| 检查项 | 结果 |
|--------|------|
| 后端 `uv sync` | ✅ 69 packages installed |
| 后端 `pytest` | ✅ 66/66 passed |
| 前端 `pnpm install` | ✅ 649 packages installed |
| 前端 `pnpm test` | ✅ 5/5 passed |
| 前端 `pnpm typecheck` | ✅ 无类型错误 |
| 前端 `pnpm dev` (Nuxt) | ✅ 启动成功 |
| 后端 WebSocket 服务 | ✅ ws://127.0.0.1:8765 可连接 |
| 前后端联调 | ⚠️ 需设置 `.env` 并手动管理后端进程 |
