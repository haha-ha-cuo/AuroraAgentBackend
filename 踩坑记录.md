# AuroraAgent 工程化与社区化踩坑记录

> 记录日期: 2026-08-28  
> 操作环境: Linux (Fedora), Python 3.14.7, Node.js 24.18.0, pnpm 11.20.0, uv 0.12.1

---

## 1. 后端：`websocket.py` 缺失（ImportError）

**现象**: 前端启动时自动拉起后端 `uv run aurora runtime --port 8765`，Python 进程直接崩溃：

```
ImportError: cannot import name 'serve_websocket' from 'aurora.agent.transport'
```

**根因**: `src/aurora/agent/transport/__init__.py` 中声明了 `from .websocket import serve_websocket`，但 `transport/` 目录下并不存在 `websocket.py` 文件。`pyproject.toml` 中的 `websockets>=15.0` 依赖已声明，`cli/commands/runtime.py` 中 `--port` 分支也会调用 `serve_websocket()`，但实现文件缺失。

**修复**: 创建 `src/aurora/agent/transport/websocket.py`，基于 `websockets 16.x` 的 `asyncio.server` API 实现 `serve_websocket(api, host, port)` 函数。

**教训**:
- `__init__.py` 的 re-export 和实际模块文件必须同步，缺少文件时 `import` 会在运行时而非安装时报错。
- 建议 CI 中增加 `python -c "from aurora.agent.transport import serve_websocket"` 作为 smoke test。

---

## 2. 前端：`spawn uv ENOENT` — 路径约定不匹配

**现象**: `pnpm dev` 启动后 Nuxt 报错：

```
ERROR [uncaughtException] spawn uv ENOENT
```

**根因**: `modules/dev-backend.ts` 约定后端位于 `../../AuroraApp`（相对于前端目录），但实际仓库名为 `AuroraAgentBackend`。默认路径不匹配导致 `cwd` 不存在，`spawn` 抛出 ENOENT。此外，如果 `uv` 不在 Node.js 子进程的 `PATH` 中也会触发此错误。

**修复**: 在前端根目录创建 `.env` 文件，设置 `AURORA_ROOT` 为绝对路径：

```
AURORA_ROOT=/mnt/Projects/AuroraAgent/AuroraAgentBackend
```

**教训**:
- 两个独立仓库的目录命名约定应显式文档化或通过环境变量解耦。
- `dev-backend.ts` 中 `fileURLToPath(new URL('../../AuroraApp', import.meta.url))` 这种硬编码的相对路径是"隐式约定"，对新贡献者极不友好。
- 建议在 README 和 `.env.example` 中明确说明仓库名和目录结构要求。

---

## 3. 端口冲突：`timeout` 命令无法清理孙进程

**现象**: 使用 `timeout 20 pnpm dev` 测试后，再次运行报错：

```
OSError: [Errno 98] error while attempting to bind on address ('127.0.0.1', 8765): address already in use
```

**根因**: `timeout` 只向直接子进程（pnpm/nuxt）发送 SIGTERM，但 `dev-backend.ts` 通过 `spawn()` 创建的 Python 子进程（`uv run aurora runtime`）作为孙进程可能存活，继续占用端口 8765。

**修复**: 手动 `kill -9 $(fuser 8765/tcp)` 清理残留进程，或改用 `VITE_RUNTIME_WS` 跳过自动拉起、手动管理后端生命周期。

**教训**:
- `spawn()` 创建的子进程在父进程被信号终止时不一定被清理（取决于 signal propagation 和进程组设置）。
- `dev-backend.ts` 中 `child.on('exit', () => { child = null })` 只监听了正常退出，未处理父进程退出时的清理。
- 建议在 `spawn` 时设置 `detached: false` 并在 `close` hook 中调用 `child.kill('SIGTERM')` + `process.on('exit', ...)` 兜底。

---

## 4. websockets 16.x API 变更

**现象**: 最初使用 `await serve(handler, host, port)` + `await asyncio.get_event_loop().create_future()` 模式启动 WebSocket 服务，虽然能绑定端口但在 `asyncio.run()` 上下文中行为不稳定。

**根因**: `websockets 16.x` 的 `serve()` 返回 `_Server` 对象，推荐使用 `async with serve(...) as server: await server.serve_forever()` 模式。旧的 `await serve(...)` + `create_future()` 模式在 Python 3.14 的 `asyncio.run()` 中存在 event loop 生命周期问题。

**修复**: 改用 `async with serve(handler, host, port) as server: await server.serve_forever()`。

**教训**:
- Python WebSocket 库的 API 跨大版本变化频繁，需锁定版本并明确测试。
- 建议在 `pyproject.toml` 中锁定 `websockets>=16,<17`。

---

## 5. `dev-backend.ts` 的 `VITE_RUNTIME_WS` 逻辑陷阱

**现象**: 设置 `VITE_RUNTIME_WS=ws://127.0.0.1:8765/ws`（与默认值相同）后，dev-backend 模块仍然自动拉起后端，导致端口冲突。

**根因**: `dev-backend.ts` 中的判断逻辑：

```ts
if (process.env.VITE_RUNTIME_WS && process.env.VITE_RUNTIME_WS !== DEFAULT_WS) return
```

只有当 `VITE_RUNTIME_WS` 存在且**不等于** `DEFAULT_WS` 时才跳过。设置为相同值不会跳过。

**修复**: 设置为不同的端口值，如 `VITE_RUNTIME_WS=ws://127.0.0.1:8766/ws`，并手动在 8766 端口启动后端。

**教训**:
- 这个"跳过"逻辑设计不合理：用户明确设置了 WS 地址，期望的是"用我指定的地址"而非"只有地址不同于默认值时才用"。
- 建议改为：只要 `VITE_RUNTIME_WS` 存在就不自动拉起后端。

---

## 6. Python 版本兼容性风险

**现象**: `pyproject.toml` 声明 `requires-python = ">=3.13"`，但实际运行环境为 Python 3.14.7。

**分析**: 当前测试全部通过（66/66），但 `asyncio` 模块在 3.13→3.14 间有 API 变更（如 `asyncio.get_event_loop()` 的 deprecation 强化）。`websockets` 和 `langgraph` 等依赖对 3.14 的支持程度未在 CI 中验证。

**建议**: CI 矩阵应包含 3.13 和 3.14 两个版本，或在 `requires-python` 中上界限制为 `<3.15`。

---

## 7. 缺少 `.env.example` 到 `.env` 的自动复制

**现象**: 新贡献者 clone 后直接运行会因缺少 `.env` 文件而失败，错误信息不明确。

**现状**: 两个仓库都有 `.env.example`，但没有自动化的 `setup` 脚本。

**建议**: 在 README 中添加明确的 `cp .env.example .env` 步骤，或提供 `make setup` / `pnpm setup` 脚本。

---

## 8. Nuxt 模块 `postinstall` 生成类型依赖 `.nuxt/`

**现象**: `pnpm install` 的 `postinstall` 脚本运行 `nuxt prepare`，如果缺少 `.env`（如 `AURORA_ROOT`），生成的 `.nuxt/` 类型可能不完整。

**根因**: `nuxt prepare` 会加载模块，`dev-backend.ts` 模块会尝试解析后端路径。如果路径不存在，虽不会直接报错，但可能影响类型生成。

**建议**: `dev-backend.ts` 中应增加路径存在性检查，避免 `nuxt prepare` 阶段的潜在问题。

---

## 总结

| # | 问题 | 严重程度 | 修复状态 |
|---|------|---------|---------|
| 1 | `websocket.py` 缺失 | **阻塞** | ✅ 已创建 |
| 2 | 后端路径硬编码不匹配 | **阻塞** | ✅ 通过 `.env` 解决 |
| 3 | `timeout` 孤儿进程 | 中 | ⚠️ 需改进 spawn 管理 |
| 4 | websockets API 变更 | 中 | ✅ 已修复 |
| 5 | `VITE_RUNTIME_WS` 逻辑陷阱 | 中 | ⚠️ 建议修改逻辑 |
| 6 | Python 版本兼容性 | 低 | ⚠️ 建议 CI 矩阵 |
| 7 | 缺少 setup 脚本 | 低 | ⚠️ 建议添加 |
| 8 | `nuxt prepare` 路径检查 | 低 | ⚠️ 建议增加检查 |
